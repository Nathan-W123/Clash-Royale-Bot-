__pycache__/
*.pyc
.venv/
checkpoints/
!checkpoints/full_pool_60m.pt
!checkpoints/README.md
runs/
!artifacts/
!artifacts/benchmark_report.json
*.egg-info/
.pytest_cache/
data/

# Personal, machine-specific screen calibration. Create it from
# configs/live_play.example.yaml; the coordinates only mean anything on the
# display they were measured on.
configs/live_play.yaml

# Training stdout captured by nohup; the real metrics live in runs/<name>/.
runs_*.log
